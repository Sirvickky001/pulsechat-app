{
  "name": "pulsechat-presence",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --coverage --testPathPattern=src/",
    "test:ci": "jest --coverage --ci --testPathPattern=src/"
  },
  "jest": { "testEnvironment": "node" },
  "dependencies": {
    "express": "^4.18.2",
    "redis": "^4.6.13"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
